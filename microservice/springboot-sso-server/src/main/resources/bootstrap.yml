
spring:
  application:
    name: springboot-sso-server
  cloud:
    config:
      uri: http://forsrc.local:12000/config
  boot:
    admin:
      url: http://forsrc.local:13000/admin

management:
  security:
    enabled: false

server:
  port: 10000
  context-path: /sso
