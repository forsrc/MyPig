spring:
  application:
    name: springboot-config-server
  boot:
    admin:
      url: http://forsrc.local:13000/admin

management:
  security:
    enabled: false

server:
  port: 12000
  context-path: /config


