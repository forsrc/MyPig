spring:
  application:
    name: springboot-admin
  cloud:
    config:
      uri: http://forsrc.local:12000/config
  boot:
    admin:
      username: forsrc
      password: forsrc
      client:
        metadata:
          user.name:     forsrc
          user.password: forsrc


server:
  port: 13000
  context-path: /admin


